name: Import TV Logos from tv-logo Repo

on:
  workflow_dispatch: # Manual trigger
  schedule:
    - cron: '0 */6 * * *' # Every 6 hours

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout your repo
        uses: actions/checkout@v3

      - name: Clone the source repo (tv-logos)
        run: |
          git clone --depth=1 https://github.com/tv-logo/tv-logos.git source-repo

      - name: Copy logos from 'countries' folder
        run: |
          mkdir -p logos
          cp -r source-repo/countries/* countries/

      - name: Commit and push changes
        run: |
          git config user.name "AutoBot"
          git config user.email "actions@github.com"
          git add countries
          git diff --cached --quiet || git commit -m "Auto update TV logos"
          git push
